# Story 3.2: TikTok Events API server-side (adapter)

**Status:** Draft  
**Epic:** 03 — Plataformas Adicionais de Ads  
**Sprint:** 2  
**executor:** @dev  
**quality_gate:** @qa  

## Story

**As a** recipient do tracking stack que roda TikTok Ads com volume significativo,  
**I want** um adapter server-side para TikTok Events API,  
**so that** conversões de compra cheguem ao TikTok mesmo quando o pixel é bloqueado.

## Acceptance Criteria

1. `functions/webhook/tiktok/[slug].js` criado seguindo exatamente a estrutura de `functions/webhook/eduzz/[slug].js` (mesma arquitetura de adapter)
2. Adapter gateado por `TIKTOK_WEBHOOK_SLUG` env var; slug errado retorna 404
3. Adapter delega o fan-out para `functions/webhook/_core.js` após parse do payload TikTok
4. `wrangler.toml.example` atualizado com `TIKTOK_WEBHOOK_SLUG = ""` documentado
5. `docs/platforms/tiktok.md` criado seguindo estrutura de `docs/platforms/eduzz.md`
6. `config/products.js` não é modificado — TikTok fan-out é tratado dentro do adapter/core no momento certo

## Tasks / Subtasks

- [ ] Ler `functions/webhook/eduzz/[slug].js` como referência estrutural (AC: 1)
- [ ] Ler `functions/webhook/_core.js` para entender o contrato de entrada do fan-out (AC: 3)
- [ ] Criar `functions/webhook/tiktok/[slug].js` (AC: 1, 2, 3)
  - [ ] Importar e usar `guardSlug` de `functions/webhook/_utils.js`
  - [ ] Parse do payload TikTok para shape normalizado
  - [ ] Delegação para `_core.js`
- [ ] Ler `wrangler.toml.example` e adicionar `TIKTOK_WEBHOOK_SLUG` (AC: 4)
- [ ] Ler `docs/platforms/eduzz.md` como referência de documentação (AC: 5)
- [ ] Criar `docs/platforms/tiktok.md` (AC: 5)

## Dev Notes

**Endpoint TikTok Events API:** `https://business-api.tiktok.com/open_api/v1.3/event/track/`

**Shape normalizado esperado por `_core.js`** (verificar no arquivo antes de implementar):
- `external_id`, `email_hash`, `phone_hash`, `fbp`, `fbc`, `gclid`, `event_name`, `value`, `currency`, `trk`

**Regra hard do projeto:** cada adapter tem seu próprio arquivo em `functions/webhook/<platform>/[slug].js`. NUNCA adicionar branching de plataforma em `_core.js`.

**`guardSlug` helper:** já implementado em `functions/webhook/_utils.js` — usar, não reimplementar.

**Nota:** TikTok Events API requer `access_token` no header Authorization. Nova env var `TIKTOK_EVENTS_API_TOKEN` necessária — documentar em `wrangler.toml.example`.

**Arquivos afetados:**
- `functions/webhook/tiktok/[slug].js` (novo)
- `wrangler.toml.example`
- `docs/platforms/tiktok.md` (novo)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-25 | 1.0 | Story criada | Orion/@aiox-master |
